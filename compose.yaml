services:
    php:
        build:
            context: .
            args:
                PHP_VERSION: ${PHP_VERSION:-8.4}
        working_dir: /srv/app
        volumes:
            - ./:/srv/app:rw,cached